use flake .
# Strip the auto-generated -rpath that contains the project path.
# Paths with spaces break the linker (and Emacs native-comp).
export NIX_LDFLAGS="$(echo "$NIX_LDFLAGS" | sed 's|-rpath.*outputs/out/lib||')"
